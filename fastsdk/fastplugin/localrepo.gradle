apply plugin: 'maven'

repositories {
    mavenCentral()
}

group='com.xx.fastsdk'
archivesBaseName = 'test'
version='0.3.3'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri('../repo'))
        }
    }
}