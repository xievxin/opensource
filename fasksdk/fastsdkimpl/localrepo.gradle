apply plugin: 'maven'

println("localrepo...")

repositories {
    mavenCentral()
}
group='com.xx.fastsdk'
archivesBaseName = 'test'
version='0.1.2'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri('../repo'))
        }
    }
}